#!/bin/bash

ROOT_DIR=$(cd "$(dirname "$0")/.." >/dev/null 2>&1 && pwd)

docker build -t malang_archlinux:latest -f "$ROOT_DIR/ArchDistrobox.dockerfile" .

distrobox create --name malangbox --image malang_archlinux:latest

distrobox enter malangbox
